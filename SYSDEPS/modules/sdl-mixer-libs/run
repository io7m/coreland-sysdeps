#!/bin/sh

. ../../sysdep-main
if [ $? -ne 0 ]
then
  exit 111
fi

f1_tmp="libs-sdl-mixer"
echo "SYSDEPS sdl-mixer-libs run create ${f1_tmp}"

f1_out="${SYSDEP_OUTDIR}/${f1_tmp}"

./sdl-mixer-libs ${f1_tmp}
cp ${f1_tmp} ${f1_out}
