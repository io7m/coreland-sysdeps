#!/bin/sh

. ../../sysdep-main
if [ $? -ne 0 ]
then
  exit 111
fi

f1_tmp="flags-sse2"
echo "SYSDEPS sse2-flags run create ${f1_tmp}"

f1_out="${SYSDEP_OUTDIR}/${f1_tmp}"

./sse2-flags ${f1_tmp} 
cp ${f1_tmp} ${f1_out}
