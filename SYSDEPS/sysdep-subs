#!/bin/sh

fatal()
{
  echo "fatal: $1" 1>&2
  exit 111
}

search_for()
{
  header="$1"
  path="$2"
  args="$3"

  for d in ${path}
  do
    if [ -f "${d}/${header}" ]
    then
      echo " -I${d} "
      return 0
    fi
  done

  # could not locate header
  return 1
}
