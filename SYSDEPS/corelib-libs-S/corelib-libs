#!/bin/sh

. ../sysdep-subs
if [ $? -ne 0 ]
then
  exit 111
fi

exec 1> "$1"

FLAGS=`try_config_bin "corelib-conf" "compile slibdir" 2>&-`
if [ "x${FLAGS}" != "x" ]
then
  echo ${FLAGS}
  exit 0
else
  error "try_config_bin failed"
fi

exit 0
