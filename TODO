http://predef.sourceforge.net/index.php

sysdeps-add:

Correctly copy 'os_ext' and subdirectories in modules!

------------------------------------------------------------------------

sysdeps-run target in sd-genmk for cross compilation convenience.

------------------------------------------------------------------------

Move sd-genmk and sd-creates (at least) out of SYSDEPS and place them
in sysdeps-* namespace.

------------------------------------------------------------------------

sysdeps-genchk for generating test programs:

  check.dat
    tiff-flags : required
    png-flags  : required
    gif-flags  : optional

  $ sysdeps-genchk check.dat
  #include <stdio.h>
  #include <stdlib.h>
  
  int
  main (void)
  {
    int return_code = EXIT_SUCCESS;
  #ifndef SD_HAVE_TIFF
    printf ("generated message about libtiff with URL, etc");
    return_code = EXIT_FAILURE;
  #endif
    return return_code;
  }

------------------------------------------------------------------------

Standardize module prelude, compiler use, etc.
  Provide template for custom modules?

------------------------------------------------------------------------

Consider making SPARC into SPARC and SPARC64:

  SPARC __sparc __sparcv8
  SPARC64 __sparcv9

------------------------------------------------------------------------

Overhaul all architectures?

Be more granular?

------------------------------------------------------------------------

Intel CPU model table.
